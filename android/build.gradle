buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.1' // Actualiza a una versión compatible
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
