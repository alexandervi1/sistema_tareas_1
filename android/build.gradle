buildscript {
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:7.4.2' // Asegúrate de usar una versión compatible
        // Otros classpath necesarios
    }
}

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}
